import {Request, Response} from "express";


const createCurrentUser = ()=>{
// 1. Check if the user already exsits
// 2. Create user if not exists.
// 3. return the user object to the calling clients

}

export default {createCurrentUser};